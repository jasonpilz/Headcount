require_relative '../lib/enrollment_parser'

class EnrollmentParserTest < Minitest::test
  # parse opens the file passed in
  # clips results to specified district
  # returns hash with years as keys, and hashes of data as values
  
end
